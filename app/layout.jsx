export const metadata = {
  title: 'OxPay — Waitlist',
  description: 'Join the OxPay waitlist.',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  )
}
